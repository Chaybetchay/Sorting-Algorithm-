# Sorting-Algorithm-
This PHP program accepts an array of integers from the user through an HTML form and sorts it in ascending or descending order using the Bubble Sort algorithm (without any built-in sorting functions). It displays: The original array The sorted array The total number of swaps performed during sorting
